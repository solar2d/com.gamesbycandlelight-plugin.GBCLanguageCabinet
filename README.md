Sources for the plugin `plugin.GBCLanguageCabinet`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.GBCLanguageCabinet" = {
            publisherId = "com.gamesbycandlelight",
        },
    },
}
```
